info:
  name: Add Task
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/api/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Test task2",
        "description": "Task 12335",
        "isOverwhelming": false,
        "subtasks": [{
        "title": "subtask 1",
        "description": "chicken"
      }]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
