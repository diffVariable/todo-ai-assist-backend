info:
  name: Patch Task
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base-url}}/api/tasks/:id"
  params:
    - name: id
      value: 69d54d25cc63b15a257be9a1
      type: path
  body:
    type: json
    data: |-
      {
        "status": "done"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
