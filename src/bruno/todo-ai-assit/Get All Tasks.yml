info:
  name: Get All Tasks
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/api/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
